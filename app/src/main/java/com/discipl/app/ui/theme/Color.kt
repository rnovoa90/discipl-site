package com.discipl.app.ui.theme

import androidx.compose.ui.graphics.Color

object AppColors {
    val background = Color(0xFF0D1117)    // near-black, main background
    val surface = Color(0xFF161B22)       // card/surface background
    val primary = Color(0xFF1A1A2E)       // deep navy
    val accent = Color(0xFF00B4D8)        // teal/electric blue — progress, CTAs
    val success = Color(0xFF06D6A0)       // green — milestones, streaks, check-ins
    val danger = Color(0xFFEF476F)        // red — relapse only
    val textPrimary = Color.White
    val textSecondary = Color(0xFF8B949E) // muted gray
}
